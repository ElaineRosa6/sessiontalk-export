# Audit fix record — 2026-08-31

This change was made on the existing dirty working tree; unrelated edits were preserved.

## Confirmed issue and change

`npm test` previously exited successfully without executing the repository's regression scripts. The package now exposes a deterministic test command covering code-block conversion, the full extraction pipeline, thinking-region recovery and widget filtering.

## Verification

- `npm test` — all four regression scripts passed, exit 0
- `npm run build` — webpack production build passed, exit 0
